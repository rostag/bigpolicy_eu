describe('LeaderEditComponent', () => {

  beforeEach(() => {
    console.log('Before test')
  })

  it('requires to enter the name' () => {

    // const lec = new LeaderEditComponent();
    // lec.leaderFormGroup = new FormControl()

    expect(true).toBe(true);
    
  });
});
